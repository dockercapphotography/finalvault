// MinimalTemplate — stub, coming soon
export default function MinimalTemplate({ gallery, images }) {
  return (
    <div className="min-h-screen flex items-center justify-center" style={{ background: '#fff' }}>
      <p style={{ color: '#6b7280' }}>{gallery.title} — Minimal template coming soon</p>
    </div>
  )
}
